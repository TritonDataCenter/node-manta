#!/usr/bin/env node
/*
 * Copyright 2019 Joyent, Inc.
 */

/*
 * The `mbucket` CLI command.
 */

process.stdout.on('error', function onStdoutError(err) {
    if (err.code === 'EPIPE') {
        process.exit(0);
    }
});

require('../lib/mbucket').main();
